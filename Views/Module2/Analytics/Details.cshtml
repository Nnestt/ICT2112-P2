@model ProRental.Domain.Entities.Analytic
@{
    ViewData["Title"] = "Analytics Details";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Analytics #@Model.Analyticsid</h2>
        <a asp-action="Index" class="btn btn-outline-secondary">← Back</a>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <span class="badge @(Model.Analyticstype?.ToString() == "DAILY" ? "bg-info" :
                                  Model.Analyticstype?.ToString() == "SUPTREND" ? "bg-warning text-dark" :
                                  "bg-success") fs-6">
                @Model.Analyticstype
            </span>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Date Range</dt>
                <dd class="col-sm-9">
                    @Model.Startdate?.ToString("yyyy-MM-dd") — @Model.Enddate?.ToString("yyyy-MM-dd")
                </dd>

                <dt class="col-sm-3">Loan Amount</dt>
                <dd class="col-sm-9">@Model.Loanamt</dd>

                <dt class="col-sm-3">Return Amount</dt>
                <dd class="col-sm-9">@Model.Returnamt</dd>

                <dt class="col-sm-3">Reference</dt>
                <dd class="col-sm-9">@Model.Refprimaryname (@Model.Refprimaryid)</dd>

                <dt class="col-sm-3">Reference Value</dt>
                <dd class="col-sm-9">@Model.Refvalue?.ToString("C")</dd>
            </dl>
        </div>
    </div>

    <!-- Generate Report from this Analytics -->
    <div class="card">
        <div class="card-header">Generate Report</div>
        <div class="card-body">
            <form method="post" asp-action="GenerateReport">
                <input type="hidden" name="refAnalyticsID" value="@Model.Analyticsid" />
                <div class="mb-3">
                    <label class="form-label">Report Title</label>
                    <input type="text" name="title" class="form-control" required
                           placeholder="e.g. March Supplier Trend Report" />
                </div>
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">Visual Type</label>
                        <select name="visualType" class="form-select">
                            <option value="Table">Table</option>
                            <option value="Bar">Bar</option>
                            <option value="Line">Line</option>
                            <option value="Pie">Pie</option>
                            <option value="Column">Column</option>
                            <option value="Area">Area</option>
                        </select>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">File Format</label>
                        <select name="fileFormat" class="form-select">
                            <option value="Pdf">PDF</option>
                            <option value="Xlsx">XLSX</option>
                            <option value="Csv">CSV</option>
                        </select>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Generate Report</button>
            </form>
        </div>
    </div>
</div>
